//import { SET_BREEDS_ARRAY } from './actions/getBreedsArray';

const initialState = {
    gameId: null,
    state: 'somestate'
}

export default (state = {}, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
}